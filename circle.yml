machine:
  node:
    version: 4.2.1

dependencies:
  pre:
   - npm install -g karma-cli
  post:
   - tsd reinstall --overwrite --save

deployment:
  staging:
    branch: master
    heroku:
      appname: teneleven
